apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grpc-greeter-claim0
  labels:
    service: grpc-greeter-claim0
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 64Mi
status: {}
