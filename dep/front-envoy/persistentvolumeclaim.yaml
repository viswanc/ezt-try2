apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: front-envoy-claim0
  labels:
    service: front-envoy-claim0
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 64Mi
status: {}
