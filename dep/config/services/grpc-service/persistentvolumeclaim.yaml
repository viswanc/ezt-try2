apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grpc-service-claim0
  labels:
    service: grpc-service-claim0
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 64Mi
status: {}
